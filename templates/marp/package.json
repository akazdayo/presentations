{
  "name": "marp-presentation",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "marp slides.md --html --allow-local-files",
    "dev": "marp slides.md --preview --allow-local-files"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^4.2.3"
  }
}
